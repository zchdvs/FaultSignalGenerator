****************************************
If installing to the Pi (or linux?), enter the following lines to the commandline:

pip install numpy

pip install scipy

pip install comtrade

sudo apt-get update

sudo apt-get upgrade

sudo apt-get install portaudio19-dev

sudo pip install pyaudio

****************************************

if installing to windows, enter the following lines to the commandline:

pip install numpy

pip install scipy

pip install comtrade

pip install pyaudio
****************************************